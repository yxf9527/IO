RandomAccessFile类是Java提供操作文件内容的类

与普通的Java中提供的IO流操作类相比 ，该类是最优秀的 提供了功能最为丰富的方法  同时可以从任意位置读写文件

参考API提供的读写方法

可根据“rw”,"r","w"参数构造读写文件的类型

文件在硬盘上是byte byte byte存储的  

打开文件开始读时   提供的指针位置point为0
每读一个字节 point增加1

不同的方法操作的字节数不同
write(int);--只写一个字节

write(char);--写一个字符 （2个字节gbk中）

记得关闭资源防止出现问题
